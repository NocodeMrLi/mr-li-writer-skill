---
name: mr-li-writer
description: "主题驱动的深度文章创作工作流。输入一个主题（可选附种子文档，或用户直接抛出核心想法/天马行空的念头），自动完成素材提取、全网检索与适配性自分析、新颖角度设计、去AI化深度写作、爆款标题生成，最终交付单文件 HTML（一键复制/下载、6 套排版主题、文末引用说明）。This skill should be used when 用户给出一个文章主题或核心想法要求写深度文章/公众号文章/爆款文章，或要求基于素材文档进行二次创作并输出排版好的 HTML。"
agent_created: true
---

# Mr.Li Writer — 主题驱动深度文章创作

## Overview

接收用户给出的文章主题，或用户提供的一段**核心想法（允许天马行空）**，或附带的种子文档，按固定工作流产出一篇**事实可验证、无广告、无个人 IP、去AI腔、角度新颖且有据**的深度中文文章，交付 Markdown 正文（含文末引用）+ 爆款标题候选 + 单文件排版 HTML。

## 硬性原则（全程有效）

1. **事实可验证**：所有关键事实必须来自检索结果且经多源交叉验证，禁止虚构数据、政策、事件。无法验证的内容明确标注或删除。
2. **内容洁净（双红线）**：严禁出现品牌词、公司名、产品名；**同时严禁出现素材里夹带的个人 IP / 博主名 / 网红名 / 个人品牌**。引用来源时只用机构/媒体类别指代（如"某机构报告""行业调研"）。
3. **角度新颖且有据**：文章应能从差异化角度看问题，但每个非主流观点必须有检索到的证据和透明推理兜底，不得为求新而编造。
4. **用户想法落地**：用户抛出的核心想法（含大胆推演）照单全收，由 AI 负责搜证、采纳可支撑的部分、诚实标注无依据的部分，最终文章是"用户想法 + AI 查证成文"的合体。
5. **去AI化**：写作与改稿全程遵循 `references/humanize-rules.md`，交稿前做一次 AI 味自评（≤3 分）。
6. **文末引用（必须带 URL）**：凡是正文用到的数据/政策/关键事实，必须在结尾"参考资料"段逐条列出（机构 + 报告名 + 发布时间 + 原始 URL）。
7. **迭代式创作**：先出大纲和文字版给用户确认方向，确认后再排版，不要在未确认前直接产出 HTML。

## 工作流程

### 第 1 步：需求确认

明确五件事（信息不足时用 AskUserQuestion 提问，一次不超过 3 个问题）：

- 主题与角度边界
- 目标读者（如 PM/产品经理/备考人群/职场人/纯小白）
- 文章用途与发布平台（公众号/小红书/知乎等）
- 是否有种子文档（docx/图片/PDF）
- **用户是否已有核心想法/观点**（哪怕是天马行空的念头，都要记下来作为种子）

### 第 2 步：种子素材提取（可选）

若用户提供种子文档，运行提取脚本：

```bash
python scripts/extract_seed.py <文件路径> [<文件路径2> ...]
```

- 支持 .docx（文字+内嵌图片 OCR）和常见图片格式（png/jpg/jpeg/bmp/webp）
- 输出结构化文本到 stdout，图片中的表格/榜单信息转写为 Markdown 表格
- **提取时主动抹掉原文里的人名/个人 IP**，只保留观点与事实
- 首次运行若缺依赖，按脚本报错提示安装（python-docx、rapidocr_onnxruntime），安装到受管 venv，禁止全局 pip install
- 无种子文档则跳过本步

### 第 3 步：全网检索 + 适配性自分析（闸口）

按 `references/research-protocol.md` 执行检索，并执行**素材适配自分析**：

- 围绕主题构造多角度关键词组（中文为主，必要时中英混合）
- 对每条候选资料做**星级溯源**（⭐⭐⭐⭐⭐ 政府/官方 → ⭐ 无法溯源），低于 ⭐⭐ 或无法溯源的一律淘汰
- 判断"适不适合本次任务"（相关性/时效性/是否夹带广告），不合适就剔除
- **若通过筛选的可信资料不足以支撑论点，立刻重新检索**（换关键词/角度/时间窗），不得用低质资料硬凑
- 关键事实至少 2 个独立来源交叉验证；记录来源、日期与 URL

### 第 4 步：新颖角度设计 + 大纲确认（必须用 AskUserQuestion）

在大纲阶段完成内部"角度创新分析"（不展示给用户，但指导写作）：

- 先用一句话总结"主流通常怎么看"
- 再选一个**差异化角度**（hidden angle / 视角转化 / 反直觉有据），并标出支撑它的证据
- 若用户已给核心想法，把想法映射到可支撑的证据上；无依据的部分标记为"推演"而非"结论"

输出文章大纲（一级结构 + 每节核心观点 + 拟引用关键事实 + 差异化角度），然后**必须用 AskUserQuestion 让用户确认**，例如：

```json
{
  "question": "这个方向 OK 吗？",
  "options": [
    {"label": "方向 OK，直接动笔"},
    {"label": "方向不 OK，我要调整"},
    {"label": "补充核心想法"}
  ]
}
```

- 用户选"不 OK"或"补充"时，继续追问具体调整意见，不要直接进正文
- 用户确认前，不进入正文写作
- 大纲文字仍然要简洁展示给用户，但确认动作必须走弹窗

### 第 5 步：正文撰写

按 `references/writing-rules.md` 与 `references/humanize-rules.md` 撰写：

- **开头铁律（先自查再写）**：第一段（甚至前两段）禁止以"根据/数据显示/报告显示"开头、禁止直接抛出核心结论、禁止堆 3 个以上数字。必须用场景/反常/真问题/个人体感把读者带进去，结论留到中后段用证据托着出来。这是最容易被一眼识破的 AI 指纹，违反即重写。
- **结构反套路 + 情感声音**：不要套固定配比的五段式，小标题长短不一、允许闲笔；要有主观态度和锋利短句，情绪有起伏，别全程一个调（详见 writing-rules.md 对应章节）
- 深度分析结构：场景/悬念引入 → 事实论证 → 观点输出（含新颖角度与透明推理）→ 行动建议
- 对比类信息优先用表格；短段落；纯 ASCII 分隔符（如 =====）
- **关键硬信息（如各地报名时间、科目清单、政策条目）必须完整呈现，不能"随便举几个例子"；只有海量数据（如招聘统计）才允许取代表性样本并说明**
- 融入增强技巧（按需）：共情点、金句、情感曲线、公众号结尾互动钩子
- 文末必须加"## 参考资料"段，逐条列出来源（机构 + 报告名 + 发布时间 + URL）
- 写完后对照 humanize-rules.md 自查 + 做一次 AI 味自评（≤3 分）
- 输出 Markdown 文字版给用户确认措辞

### 第 6 步：爆款标题

按 `references/title-rules.md` 生成 5-10 个候选标题，按推荐度排序并给出推荐理由。

### 第 7 步：HTML 排版交付（必须用 AskUserQuestion 确认）

用户确认文字版后，先用 AskUserQuestion 让用户选择/确认：

- 用哪个标题（列出候选 + 推荐项）
- 用哪个主题（列出 6 套主题及适用场景，默认根据平台推荐）
- 是否直接交付 HTML

示例弹窗结构：

```json
{
  "questions": [
    {"header": "标题", "question": "用哪个标题？", "options": [{"label": "标题1（推荐）"}, {"label": "标题2"}, {"label": "自定义"}]},
    {"header": "主题", "question": "用哪个排版主题？", "options": [{"label": "minimal-gold（推荐）"}, {"label": "business-blue"}, {"label": "其他"}]},
    {"header": "交付", "question": "确认排版交付？", "options": [{"label": "确认，出 HTML"}, {"label": "先不排，我再改改文字"}]}
  ]
}
```

确认后运行渲染脚本：

```bash
python scripts/build_html.py <正文.md> -t <主题名> -o <输出.html> --title "<文章标题>"
```

- 内置 6 套主题（CSS 存于 `assets/themes/`，可用 `--list-themes` 查看）：
  - `minimal-gold`：黑白金极简风（默认，公众号深度文）
  - `business-blue`：商务深蓝报告风（政策解读/行业分析）
  - `magazine-warm`：暖色杂志风（故事性/人物稿）
  - `fresh-green`：清新绿（小红书/知乎适配）
  - `ink-scholar`：水墨学者风（知乎深度/文化类，衬线留白）
  - `card-modern`：卡片现代风（圆角卡片，小红书/产品文）
- 若用户未指定主题，根据发布平台从 writing-rules.md 的平台适配表推荐一个并说明理由
- 产物为单文件 HTML：零外部依赖、一键复制正文、一键下载，顶部有工具栏
- 用 present_files 交付 HTML 文件

## Resources

### scripts/

- `extract_seed.py` — 种子文档提取：docx 文字 + 图片 OCR（RapidOCR），图片表格转 Markdown 表格，提取时自动去人名
- `build_html.py` — 零依赖 Markdown→单文件 HTML 渲染器，6 主题、一键复制/下载

### references/

- `research-protocol.md` — 检索与事实验证、素材适配自分析（星级溯源）、去个人 IP 规则、文末引用规范（含 URL）
- `writing-rules.md` — 深度写作规范、开头铁律（禁数据轰炸/结论前置）、结构反套路、情感与声音、新颖角度有据、用户想法落地机制、共情/金句/互动钩子、多平台适配、关键信息完整性规则
- `humanize-rules.md` — 去AI化写作规则（中英文 AI 腔清单 + AI 味自评打分）
- `title-rules.md` — 爆款标题创作规则

### assets/

- `themes/*.css` — 6 套 HTML 排版主题样式表，新增主题只需添加一个 CSS 文件并在 build_html.py 的 THEMES 字典注册
